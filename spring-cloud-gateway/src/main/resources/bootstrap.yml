spring:
  application:
    name: spring-cloud-gateway
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
